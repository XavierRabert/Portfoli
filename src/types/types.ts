export type projectDetails = {
  imgSrc: string;
  imgName: string;
  webLink: string;
  text: string;
  textLink?: string;
  showProject: boolean;
};

export type experienceDetails = {
  name: string;
  text: string[];
};
