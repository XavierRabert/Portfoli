---
import { about } from "@/data/info.json";
---

<section id="about">
  <h2 class="text-3xl font-bold text-x-blue mb-6">Qui sóc?</h2>
  {about.map((item) => <p class="pt-1">{item}</p>)}
</section>
